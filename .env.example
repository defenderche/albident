# Скопируй в .env.local и заполни. .env.local gitignored.

# Публичные переменные (доступны на клиенте) — префикс NEXT_PUBLIC_
NEXT_PUBLIC_SITE_URL=https://example.com

# Серверные (используются только в Server Actions и серверных компонентах)
# Resend — отправка заявки на email клиники
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
CLINIC_EMAIL=
