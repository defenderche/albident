---
name: audit-block-a
branch: fix/audit-block-a
status: completed
created: 2026-06-02
completed: 2026-06-02
---

# План: Дефекты из аудита — блок A

## Цель
Устранить 4 однозначных дефекта, найденных в дизайн/структурном аудите страниц:
рассинхрон контента, A11y и навигации.

## Контекст
Аудит всех страниц (5 областей). Блок A — «настоящие дефекты», не вкусовщина.
Часы работы: канон выбран — **Пн–Сб 10:00–20:00, Вс выходной** (site.ts остаётся, карта подтягивается).

## Шаги
- [x] 1. `/privacy`: список собираемых данных привести к реальным полям формы — убрать `email`, добавить «город/страну» и «удобное время». Все 3 языка (`content/privacy.ts`).
- [x] 2. Часы работы: синхронизировать `design-map/index.html` под `content/site.ts` (Пн–Сб 10–20, Вс выходной).
- [x] 3. A11y формы записи: `aria-required` обязательным полям (имя, телефон, согласие) в `BookingForm.tsx`.
- [x] 4. Активный пункт навигации: подсветка + `aria-current="page"` через клиентский `NavLinks.tsx` (десктоп) и `HeaderMobile.tsx` (паттерн уже есть в дизайн-карте). Чистая утилита `isActivePath` + тест.
- [x] Проверка: tsc, lint, test (65 passed), build; на живом сервере подтверждены aria-current и privacy.

## Критерии готовности
- Privacy перечисляет ровно те поля, что собирает форма.
- Часы одинаковы в коде и дизайн-карте.
- Обязательные поля формы программно помечены required.
- Активный пункт меню подсвечен и помечен aria-current на всех страницах.
- Зелёные lint / tsc / test.
